# Slack

Automatically create a list of connections to Slack via the Web API

## How to Run with Docker

### Build the Docker image

```bash
    docker build -t my-python-app .
```

### Run the Docker container

```bash
    docker run my-python-app  -m <your-text>
```
